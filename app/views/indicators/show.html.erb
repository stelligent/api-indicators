<% title @indicator.name %>
<%= content_tag :h2, @indicator.name %>

<table class="table" style="width: auto;">
  <thead><tr><td><h4>History</h4></td></tr></thead>
  <tbody>
    <% @indicator.get_history.each do |event| %>
    <%= history_row event %>
    <% end %>
  </tbody>
</table>
